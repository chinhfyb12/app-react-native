const BaseUrl = {
  dev: 'http://localhost:8080',
};

const Endpoint = {
  signup: '/auth/signup',
  login: '/auth/login',
  profile: '/users/profile',
  products: '/products',
  orders: '/orders',
};

export {BaseUrl, Endpoint};
